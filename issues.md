# 代码审查问题清单 — dev 分支排序算法

- **审查对象**：`sort/bubble-sort.js`、`sort/quick-sort.js`、`sort/merge-sort.js`
- **分支**：`dev`
- **审查日期**：2026-09-08
- **审查结论**：三种排序算法逻辑基本正确，但存在工程质量与健壮性方面的多处问题，需修复。

---

## 一、通用问题（影响三个文件）

| 编号 | 问题 | 说明 | 影响 |
|------|------|------|------|
| G1 | 缺少输入校验 | 三个函数均未校验 `arr` 是否为数组，传入 `null`/`undefined` 会抛出 `Cannot read properties of null (reading 'length')` 这类难以定位的错误 | 健壮性 |
| G2 | 使用 `var` 声明变量 | 全部使用 `var`，存在变量提升与函数作用域问题，应使用 `let`/`const` | 可维护性 |
| G3 | 缺少 JSDoc 注释 | 函数无参数类型、返回值、时间/空间复杂度说明 | 可读性 |
| G4 | 缺少模块导出 | 函数未通过 `module.exports` 导出，无法被其他模块复用，也无统一入口 | 可复用性 |
| G5 | 缺少测试用例 | 无测试文件，无法验证正确性与边界情况（空数组、单元素、重复、已排序、逆序、非法输入） | 质量保障 |

---

## 二、bubble-sort.js

| 编号 | 问题 | 说明 | 修复方案 |
|------|------|------|---------|
| B1 | 修改原数组（副作用） | 直接对入参数组原地交换，调用方数据被意外修改 | 先 `slice()` 拷贝，返回新数组 |
| B2 | 内层循环未优化 | `j < arr.length - 1` 每轮都遍历到末尾，已排序的尾部被重复比较 | 改为 `j < n - 1 - i` |

---

## 三、quick-sort.js

| 编号 | 问题 | 说明 | 修复方案 |
|------|------|------|---------|
| Q1 | 基准点固定取首元素 | 对近乎有序的数据会退化为 O(n²)，递归深度增大 | 随机选取基准 |

---

## 四、merge-sort.js

| 编号 | 问题 | 说明 | 修复方案 |
|------|------|------|---------|
| M1 | `merge` 使用 `shift()` | `Array.prototype.shift()` 为 O(n)，导致 `merge` 为 O(n²)，整体退化为 O(n²·log n)，丧失归并排序 O(n log n) 的优势 | 用索引指针替代 `shift()` |
| M2 | `merge` 缺少访问控制 | `merge` 作为内部辅助函数却与公开 API 同级声明并被隐式暴露 | 用 IIFE 将 `merge` 限制为私有，仅导出 `mergeSort` |

---

## 五、修复优先级

1. **高**：G1（输入校验）、M1（`shift()` 性能退化）— 影响正确性与性能
2. **中**：B1（副作用）、B2（循环优化）、Q1（基准退化）、M2（访问控制）
3. **低**：G2（`var`→`let/const`）、G3（注释）、G4（导出）、G5（测试）

修复后补齐 `sort/index.js`（统一入口）与 `sort/test.js`（测试），并保证 `node sort/test.js` 全部通过。
