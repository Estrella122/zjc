# zjc 项目制作文档

> 本文档记录 `Estrella122/zjc` 仓库从 SSH 环境配置、博客开发、排序算法实现、代码审查修复到合并推送的完整制作过程，可作为复现与维护参考。

- **仓库地址**：https://github.com/Estrella122/zjc
- **制作日期**：2026-09-08
- **运行环境**：macOS（Darwin 25.1.0）、git 2.50.1、OpenSSH 10.0、Node.js
- **技术栈**：HTML5 + CSS + JavaScript（博客）、Node.js CommonJS（排序算法）

---

## 一、项目概述

本项目包含两个部分：

1. **个人技术博客**：纯 HTML5 + CSS + JavaScript 实现，零依赖，支持响应式布局与暗黑模式切换。
2. **排序算法模块**：在 `sort/` 目录下用 JavaScript 实现冒泡、快速、归并三种排序算法，含统一入口与单元测试，并经历了一轮「实现 → 代码审查 → 修复」的完整流程。

---

## 二、SSH 连接 GitHub 配置

### 2.1 问题诊断

本机使用 Clash 代理（`127.0.0.1:7897`），DNS 工作在 **fake-ip 模式**：

```
github.com     → 198.18.0.117   (fake-ip 虚拟地址)
ssh.github.com → 198.18.0.239
```

直接 SSH 连接 GitHub 22 端口超时：

```
$ ssh -T git@github.com
Connection timed out during banner exchange
Connection to 198.18.0.117 port 22 timed out
```

原因：代理未转发 22 端口的 SSH 流量，导致连接被阻断。

### 2.2 解决方案

GitHub 提供 **443 端口的 SSH 入口**（`ssh.github.com:443`），通常不被封锁。测试确认 TCP 握手成功：

```
$ ssh -T -p 443 git@ssh.github.com
Hi Estrella122! You've successfully authenticated, but GitHub does not provide shell access.
```

### 2.3 操作步骤

**1）生成 ed25519 SSH 密钥**

```bash
mkdir -p ~/.ssh && chmod 700 ~/.ssh
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519 -N "" -C "donghanxin@github"
```

**2）配置 `~/.ssh/config`，让 `github.com` 走 443 端口**

```
Host github.com
  HostName ssh.github.com
  User git
  Port 443
  IdentityFile ~/.ssh/id_ed25519
  AddKeysToAgent yes
  UseKeychain yes
```

```bash
chmod 600 ~/.ssh/config
ssh-add --apple-use-keychain ~/.ssh/id_ed25519
```

**3）添加公钥到 GitHub**

复制 `~/.ssh/id_ed25519.pub` 内容，在 https://github.com/settings/ssh/new 添加为 Authentication Key。

**4）测试连接**

```bash
$ ssh -T git@github.com
Hi Estrella122! You've successfully authenticated, but GitHub does not provide shell access.
```

> ⚠️ 注意：因使用 fake-ip 模式代理，**SSH 能通的前提是代理处于开启状态**。关闭代理后 `198.18.x.x` 为不可路由的虚拟地址，连接会失败。

---

## 三、克隆仓库

```bash
$ cd ~ && git clone git@github.com:Estrella122/zjc.git
Cloning into 'zjc'...
warning: You appear to have cloned an empty repository.
```

仓库初始为空，所有内容从零构建。

---

## 四、个人技术博客开发

### 4.1 功能特性

- 📱 响应式设计，适配手机与桌面
- 🌙 暗黑模式切换（跟随系统 + localStorage 记忆偏好）
- ⚡ 零依赖，纯原生 HTML5/CSS/JS
- 📝 文章列表（JS 动态渲染）+ 文章详情页

### 4.2 开发步骤（4 个增量 commit）

| Commit | Hash | 内容 |
|--------|------|------|
| 1 | `e3c5155` | 初始化项目结构：`README.md` + 基础 `index.html` |
| 2 | `d5ac7f4` | 添加响应式样式与设计系统：`css/style.css`（CSS 变量驱动亮/暗主题） |
| 3 | `e2474b3` | 添加交互：`js/main.js`（暗黑模式切换 + 文章列表渲染） |
| 4 | `c463289` | 添加首篇博客文章：`posts/hello-world.html` |

### 4.3 博客目录结构

```
├── README.md              # 项目说明
├── index.html             # 首页（文章列表 + 主题切换）
├── css/
│   └── style.css          # 样式与设计系统（亮/暗主题）
├── js/
│   └── main.js            # 暗黑模式 + 文章列表渲染
└── posts/
    └── hello-world.html   # 首篇博客文章
```

### 4.4 本地预览

```bash
cd ~/zjc && python3 -m http.server 8000
# 浏览器打开 http://localhost:8000
```

---

## 五、排序算法开发（dev 分支）

### 5.1 创建 dev 分支

```bash
git switch -c dev
```

### 5.2 实现三种排序算法

在 `sort/` 目录下创建三个文件（初版）：

- `sort/bubble-sort.js` — 冒泡排序
- `sort/quick-sort.js` — 快速排序
- `sort/merge-sort.js` — 归并排序

提交并推送：

```bash
git add sort/
git commit -m "实现三种排序算法：冒泡、快速、归并"
git push -u origin dev
```

对应 commit：`37fdefd`

---

## 六、代码审查与修复

### 6.1 审查结论

三种排序算法逻辑基本正确，但存在工程质量与健壮性方面的多处问题。审查结果记录于 `issues.md`。

### 6.2 发现的问题

**通用问题（影响三个文件）**

| 编号 | 问题 | 影响 |
|------|------|------|
| G1 | 缺少输入校验，传入 `null`/`undefined` 抛出难以定位的错误 | 健壮性 |
| G2 | 使用 `var` 声明，存在变量提升与作用域问题 | 可维护性 |
| G3 | 缺少 JSDoc 注释 | 可读性 |
| G4 | 缺少模块导出，无法复用，无统一入口 | 可复用性 |
| G5 | 缺少测试用例 | 质量保障 |

**各文件问题**

| 编号 | 文件 | 问题 | 修复方案 |
|------|------|------|---------|
| B1 | bubble-sort | 修改原数组（副作用） | `slice()` 拷贝后排序，返回新数组 |
| B2 | bubble-sort | 内层循环未优化，重复比较已排序尾部 | `j < n - 1 - i` |
| Q1 | quick-sort | 基准点固定取首元素，有序数据退化为 O(n²) | 随机选取基准 |
| M1 | merge-sort | `merge` 使用 `shift()`（O(n)），整体退化为 O(n²·log n) | 用索引指针替代 |
| M2 | merge-sort | `merge` 与公开 API 同级声明被隐式暴露 | 用 IIFE 设为私有 |

### 6.3 修复实现

- 三个算法文件均加入 `Array.isArray` 校验并抛 `TypeError`
- 全部改用 `let`/`const`，补充 JSDoc 与复杂度说明
- 通过 `module.exports` 导出，新增 `sort/index.js` 统一入口
- `bubbleSort` 拷贝原数组、优化内层循环
- `quickSort` 随机选取基准
- `mergeSort` 用 IIFE 封装私有 `merge`，索引指针合并
- 新增 `sort/test.js` 单元测试

### 6.4 测试结果

```bash
$ node sort/test.js
✅ bubbleSort · 常规 / 空数组 / 单元素 / 全重复 / 已排序 / 逆序 / 非数组(null) / 非数组(undefined)
✅ quickSort  · (同上 8 项)
✅ mergeSort  · (同上 8 项)

总计：24 通过，0 失败
```

测试覆盖：常规、空数组、单元素、全重复、已排序、逆序、非法输入，并验证「不修改原数组」。

修复提交：`a460a0d` — "依据 issues.md 修复排序算法问题并补充测试"

### 6.5 修复后 sort/ 目录

```
sort/
├── bubble-sort.js   # 冒泡排序（已修复）
├── quick-sort.js    # 快速排序（已修复）
├── merge-sort.js    # 归并排序（已修复）
├── index.js         # 统一入口
└── test.js          # 单元测试
```

---

## 七、合并与推送

将 `dev` 合并回 `main` 并推送至 GitHub（fast-forward 合并）：

```bash
git checkout main
git merge dev          # Fast-forward，c463289..a460a0d
git push origin main
```

合并后 `main` 与 `dev` 均指向 `a460a0d`，远程仓库已同步。

---

## 八、最终项目结构

```
zjc/
├── 制作文档.md            # 本文档
├── issues.md              # 代码审查问题清单
├── README.md              # 项目说明
├── index.html             # 博客首页
├── css/
│   └── style.css          # 博客样式
├── js/
│   └── main.js            # 博客交互脚本
├── posts/
│   └── hello-world.html   # 博客文章
└── sort/
    ├── bubble-sort.js     # 冒泡排序
    ├── quick-sort.js      # 快速排序
    ├── merge-sort.js      # 归并排序
    ├── index.js           # 排序模块入口
    └── test.js            # 排序模块测试
```

---

## 九、完整提交历史

```
a460a0d  依据 issues.md 修复排序算法问题并补充测试   (main, dev)
37fdefd  实现三种排序算法：冒泡、快速、归并
c463289  添加首篇博客文章《Hello World》
e2474b3  添加暗黑模式切换与文章列表渲染交互
d5ac7f4  添加响应式页面样式与设计系统
e3c5155  初始化博客项目结构
```

| 阶段 | 分支 | Commit |
|------|------|--------|
| 博客开发 | main | `e3c5155` → `d5ac7f4` → `e2474b3` → `c463289` |
| 排序算法初版 | dev | `37fdefd` |
| 审查修复 | dev | `a460a0d` |
| 合并推送 | main | fast-forward 至 `a460a0d` |

---

## 十、关键命令速查

```bash
# SSH 测试
ssh -T git@github.com

# 克隆 / 更新
git clone git@github.com:Estrella122/zjc.git
git pull

# 分支操作
git switch -c dev          # 创建并切换 dev
git checkout main          # 切回 main
git merge dev              # 合并 dev 到当前分支
git push -u origin dev     # 推送 dev
git push origin main       # 推送 main

# 排序算法测试
node sort/test.js

# 博客本地预览
python3 -m http.server 8000
```

---

## 十一、后续可扩展

- **GitHub Pages 部署**：仓库 Settings → Pages → Source 选 `main` 分支，即可在线访问博客
- **新增博客文章**：在 `posts/` 下新建 HTML，并在 `index.html` 的 `window.POSTS` 数组添加元数据
- **新增排序算法**：在 `sort/` 下新增文件并导出，在 `index.js` 注册、`test.js` 补测试
- **CI 自动测试**：添加 GitHub Actions，在推送时自动运行 `node sort/test.js`
- **分支清理**：dev 已合并，可执行 `git push origin --delete dev && git branch -d dev` 删除
